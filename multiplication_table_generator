
"""
Multiplication Table Generator

This program generates a multiplication table for a given base number
and a specified number of times.
"""

"""
# Option 1: Prompt the user twice for input
base_num = int(input("Enter a base number: "))
times = int(input("Enter number of times it multiplies: "))

for num in range(1, times + 1): # Run from time 1 to N (inclusive)
    total = base_num * num # Calculating
    print(f"{base_num} * {num} = {total}") # Print the total

# Print the final result
print(f"\nThe result of {base_num} * {times} is {total}")
"""
# Option 2: Prompt the user once for both inputs(base number and times)
base_num, times = map(int, input("Enter a base number and times it multiplies, separate by a space: ").split())
for num in range(1, times + 1): # Run from time 1 to N (inclusive)
    total = base_num * num # Calculating
    print(f"{base_num} * {num} = {total}") # Print the total

# Print the final result
print(f"\nThe result of {base_num} * {times} is {total}")
